# 统一用 LF 入库并检出。
# 团队有 Windows 也有 macOS，不定死的话同一个文件会因为换行符产生整文件 diff——
# commit 历史是要给评委翻的，不能被这种噪声淹掉。
* text=auto eol=lf

# 二进制，不做换行符转换
*.png    binary
*.jpg    binary
*.jpeg   binary
*.webp   binary
*.ico    binary
*.woff   binary
*.woff2  binary
*.mp4    binary
*.pbf    binary
*.zip    binary

# 生成物，评审 diff 时折叠掉，别刷屏
package-lock.json  linguist-generated=true -diff
