# CITY OS

爱化身 CITY OS 黑客松参赛项目。城市安全场景的可运行 Demo。

## 当前开发基线（必须先核对）

- 正式开发目录是 `C:\Users\k\Documents\ChatGPT\cityos正式版`，当前开发分支是 `codex/cityos-command-workbench`。
- `fa1ba5def223d27c4881092902daae69450a1e1e`（短 SHA：`fa1ba5d`）是当前 UI 开发基线。后续代码必须基于该提交或其后代继续，不得从旧备份目录、旧分支或更早提交另起修改。
- `https://cityos-command-workbench.vercel.app/#/resources` 当前生产功能基线已更新到 `b96077f274996bbf20b4bf52daad30aebf9009c6`，包含 `fa1ba5d` UI 改版及其后续调度、路线与报告页内编辑改动。后续如本地 HEAD 继续前进，仍以当前分支后代为准，不得用更早的线上或备份产物反向覆盖开发树。
- 开始修改前必须检查 `git status --short --branch`，并运行 `git merge-base --is-ancestor fa1ba5d HEAD`。若工作区不干净、祖先校验失败或目录不符，立即停止并报告，不得自行 `reset`、`checkout` 或迁移改动。

## 一句话

接到火情警单后，自动补齐建筑和资源信息，生成多个响应方案并比较各自后果，由人工确认后生成协同任务。

## 产品定义优先级

1. 飞书《[City OS修改方案](https://xqmqf98k8t.feishu.cn/wiki/YusEwPrO5iLdpCklhvecFgNWnYh)》是当前最高优先级的产品定义与验收基准。
2. 当系统、旧本地文档或既有实现与修改方案冲突时，系统与本地文档向修改方案收敛；不得反向修改方案来迁就现状。
3. 修改方案中的“态势趋势预测”按六块 AI Brief 呈现，但必须标明它是基于当前输入和显式假设的条件推演，不代表事故发生概率预测。
4. 主看板可以同级展示 119、110、120、交通、重大布防的事件、指标与入口；同级呈现不自动等同于五套独立一级模块。

## 最重要的一条边界

**我们不做火情检测，不建城市感知网络。**

事件通过标准接口从既有系统（119 接警、物业上报、烟感、市民上报）进来。我们只做**事件已知之后**的部分：态势补齐 → 方案推演 → 资源调度 → 执行反馈。

这条是团队反复讨论后定的，理由有三：
1. 可见光烟火识别是成熟商用产品，供应商一堆，做这个是红海
2. 热成像在街面几乎零部署，做不了
3. 爱化身在侨银项目里也没做感知层——L1 的无人机和摄像头是客户资产，我们建的是 L2–L5

**任何时候如果代码或文案里出现"识别火焰""检测火情""视频分析"，都是错的。**

## 产品结构

```
一级：城市安全
├─ 二级：消防（119）—— 做细做精，主链路
└─ 二级：110 —— 只做前端展示，不做真实逻辑
    ├─ 三级：刑事民事警情
    └─ 三级：交通调度
```

- **120 不单独做**，医疗调度嵌进 119 和 110
- **重大布防**（演唱会/漫展防踩踏这类）不单独设模块，做成主看板上的一个功能点

## 差异化在哪

对标的是公司自己的侨银项目。侨银有四类闭环：AI 巡检、智能调度、质量考核、经营复盘——**全部是事后和执行，没有一个是事前比较多个方案**。

我们补的是**第五个闭环：推演**。

对外统一口径：**我们不预测事故发生，只计算既定选项的后果。**

- 预测 =「接下来会发生什么」→ 三阶段能力，不碰
- 推演 =「如果这么做会变成什么样」→ 给定假设下的确定性计算，一阶段就能做

## 技术栈

| 用途 | 选型 | 备注 |
|---|---|---|
| 前端 | React + TypeScript + Vite | |
| UI 组件 | shadcn/ui + Tailwind | 组件复制在 src/components/ui/，可以直接改 |
| 地图渲染 | MapLibre GL JS | 不用 Mapbox（要 token）。建筑用原生 `fill-extrusion` |
| 地理可视化 | deck.gl | TripsLayer 路径动画、PolygonLayer 覆盖缺口、ArcLayer 调度关系 |
| 底图瓦片 | OpenFreeMap | 免费、无需 API key |
| 楼层剖面 | 原生 SVG | 零依赖，不引图表库 |
| 路径 / 等时圈 / 时间矩阵 | Valhalla | Docker，配置在 services/valhalla/ |
| 空间计算 | GeoPandas + Shapely | Python，脚本在 scripts/ |
| 可解释规则 | json-logic | 配置即规则，不用黑盒模型 |
| 图表 | ECharts | |
| Agent 编排 / RAG | 公司 Agentrix / DataOS | 不引入第三方 agent 框架 |

### 明确不用

Kafka、LangGraph 类 agent 框架、OR-Tools、PostGIS。理由都是同一个：17 天的项目撑不起它们的引入成本。

## 代码约定

- **推演逻辑全部放 `src/engine/`，必须是纯函数**，不依赖 React、不读全局状态。这部分最好测也最适合 AI 写。
- **所有模拟数据在 UI 上必须有「模拟」标注。**「真实与诚信」是比赛的正式评分维度。
- **不做三维建模。**视觉方案是「城市尺度 2.5D 挤出 + 建筑尺度 SVG 剖面」，详见 `docs/视觉方案.md`。任何时候如果要引入 Cesium / Three.js / 3D 模型资产，都是走错了方向。
- **已确认与待核实必须用不同形状编码**（实心 vs 虚线描边），不能只靠颜色区分。
- 时间统一用 Unix 秒（`number`），不要传 Date 对象
- 坐标统一用 `[lng, lat]`（GeoJSON 顺序），不要用 `[lat, lng]`
- **不要新增依赖，先问我**
- 不要自作主张改架构、重命名文件、动其他模块

## Commit 规范

**这一节是给 AI 执行的，不用每次问我。**

`commit` 历史是交付物，不是过程垃圾——「开发过程留痕（commit 历史、CLAUDE.md 演进）」是「AI Native 能力」的评分依据，评委会翻。按下面写。

### AI 的自主权限

| 动作 | 权限 |
|---|---|
| 完成一个可独立验证的改动后 `commit` | **直接做，不用问** |
| `push` 到远端 | **先问** |
| `git init` / 建分支 / 合并 | 先问 |
| `--amend`、`rebase`、`reset --hard` 已提交的东西 | **不做** |
| `--no-verify` 跳过钩子 | **不做** |

「可独立验证」= `npm run typecheck` 通过，且这个改动本身讲得清是在做什么。**typecheck 不过不许提交。**

### 粒度

一个 commit 一件事。不要攒一天的活儿一次性提交，也不要把「加了个函数」拆成三个 commit。

以下必须单独成 commit，不与其他改动混在一起：

- `docs/接口契约.md` 的任何改动（它是共同依赖，见 AGENTS.md）
- 依赖增删（`package.json` / `package-lock.json`）
- 纯格式化 / 重命名

### 格式

```
<type>(<scope>): <中文描述>

<正文，可选>

Co-Authored-By: ...
```

**type**

| type | 用于 |
|---|---|
| `feat` | 新功能 |
| `fix` | 修 bug |
| `docs` | 文档 |
| `refactor` | 重构，行为不变 |
| `test` | 测试 |
| `build` | 依赖、构建配置、脚手架 |
| `chore` | 杂项 |

**scope**（对齐 AGENTS.md 的模块边界，可省略）

`engine` / `agent` / `components` / `pages` / `data` / `services` / `docs`

**描述**

- 中文，祈使句，30 字以内，结尾不加句号
- 写「做了什么」，不要写「改了哪几个文件」——文件列表 `git` 自己有
- ❌ `fix(engine): 修改了 eta.ts 和 coverage.ts`
- ✅ `fix(engine): Valhalla 超时后回退直线估算并置 degraded`

**正文**（涉及判断取舍时必须写）

说清**为什么这么选、放弃了什么、有什么已知限制**。这部分是写给评委和 8/24 之后接手的人看的。踩过的坑、试过不行的方案，都写进去——比赛评的是判断力，不是代码行数。

### 署名

AI 写的 commit **必须**带对应的 trailer：

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

Codex 用它自己的署名。**不要去掉**——这是「AI Native 能力」最直接的证据。

反过来，**人写的东西不要冒充 AI 写的，AI 写的也不要抹掉署名**。「真实与诚信」是评分维度，commit 历史同样适用。

### 绝对不提交

`node_modules/` `dist/` `data/raw/` `services/valhalla/valhalla_tiles/` `.env`

`.gitignore` 已覆盖。提交前扫一眼 `git status`，别用 `git add -A -f`。

## 数据边界

**真实数据**：城市底图、路网、医院/派出所 POI、建筑轮廓
**模拟数据**：消防站能力、消火栓、避难场所、所有资源实时状态、事件流

详见 `docs/数据边界.md`，里面有实测数字。**改任何和数据相关的文案前必读。**

## 关键文档

| 文档 | 什么时候看 |
|---|---|
| `docs/接口契约.md` | 改数据结构前必读 |
| `docs/数据边界.md` | 写任何涉及"数据是真是假"的文案前必读 |
| `docs/视觉方案.md` | 写任何 UI 前必读（含色板、图层映射、落地顺序） |
| `docs/验收清单.md` | 判断某个模块做没做完 |

## 常用命令

```bash
npm run dev          # 开发服务器
npm run typecheck    # 类型检查，提交前跑
npm run build        # 构建

# Valhalla
docker compose -f services/valhalla/docker-compose.yml up -d
```

## 比赛背景

- **抢跑期**：8/15 – 8/23（命题未发布，先搭骨架）
- **正赛**：8/24 – 8/31，7 天，3 支队伍同题竞争
- **8/24 才发布命题**，可能与当前方向有出入，届时用已有骨架适配，不重做

**评审五维**：产品判断、创造速度、AI Native 能力、带队能力、真实与诚信。
官网写死的口号：DEMO > PPT，USERS > OPINIONS，DATA > STORIES。

**交付物包含「技术判断」**——数据可得性的实测结论本身就是交付物，不只是内部参考。

## 团队

| 人 | 角色 |
|---|---|
| 康莹 | 技术负责 + 前端 + 推演引擎 |
| 王君梅 | 产品定义 + 对外叙事 |
| 周芷涵 | 火情模块业务设计 + 110 调研 |
| 王溢婧 | UIUX 支援 + 一线调研 |
| 白光成 | 8/21 入职，建议接 110 展示页 |
| 荆渤韬 | 决策 |

## 两条红线

1. **D5（8/28）结束时黄金路径必须能从头跑到尾**，哪怕每步都是简化版。之后只打磨不加功能。
2. **必须有离线可运行版本和完整录屏**，不依赖现场网络。
